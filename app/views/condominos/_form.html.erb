<%= form_for(@condomino) do |f| %>
  <% if @condomino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@condomino.errors.count, "error") %> prohibited this condomino from being saved:</h2>

      <ul>
      <% @condomino.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :unidade %><br />
    <%= f.text_field :unidade %>
  </div>
  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
